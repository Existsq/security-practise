eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761
spring:
  application:
    name: eureka
  main:
    banner-mode: off

logging:
  level:
    root: error